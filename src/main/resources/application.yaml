server:
  port: 8084
  servlet:
    context-path: /identity-system

spring:
  datasource:
    url: "jdbc:sqlserver://localhost:1733;databaseName=identity-api;encrypt=true;
    trustServerCertificate=true;"
    username: sa
    password: sa@@1234567
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

key:
  internal: mynameisphoebe

service:
  app:
    user-service: http://localhost:8080/user
